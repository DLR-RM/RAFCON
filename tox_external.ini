[tox]
envlist = py27,py35,py36,py37

[testenv]
deps = 
    pycairo<2.0.0,>=1.10.0
    pytest
setenv =
    PIP_DISABLE_VERSION_CHECK = 1
    RAFCON_START_MINIMIZED = 1
    RAFCON_LIB_PATH = {toxinidir}/share/libraries
commands = pytest -vx -m "(core or gui or share_elements) and not unstable"

