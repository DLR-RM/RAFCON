
def execute(self, inputs, outputs, gvm):
    from_states = ['BHXQIF', 'IISOYQ', 'BHXQIF', 'JFQYZU']
    #for i, data_flow in enumerate(self.parent.data_flows.values()): 
    #    if data_flow.from_state == from_states[i]:
    #        self.logger.info("Data-Flow is correct {}".format(data_flow))
    #    else:
    #        self.logger.error("Data-Flow is not correct {}".format(data_flow))
    return 0
